# 📜 SegmentHeader 滚动行为修改

## 🎯 修改内容

将 SegmentHeaderView 从**吸顶模式**改为**跟随滚动模式**。

---

## 🔧 修改的代码

### 之前（吸顶模式）
```swift
header.pinToVisibleBounds = true  // ❌ Header 会吸附在顶部
```

### 之后（跟随滚动）
```swift
header.pinToVisibleBounds = false  // ✅ Header 跟随内容一起滚动
```

---

## 📊 两种模式对比

### 模式 1: 吸顶（Sticky Header）
**`pinToVisibleBounds = true`**

```
向下滚动时：
┌────────────────────────────┐
│ Section 0: 静态头部         │
│ (向上滚出屏幕)             │
├────────────────────────────┤
│ [Overview][Details]...     │ ← 吸附在顶部，不动
├────────────────────────────┤
│ Page 内容继续向上滚动       │
│ ...                        │
└────────────────────────────┘
```

**特点**:
- ✅ Header 始终可见
- ✅ 快速切换页面
- ❌ 占用屏幕空间
- 📱 **常见于**: 微博详情页、小红书详情页

---

### 模式 2: 跟随滚动（Scrolling Header）
**`pinToVisibleBounds = false`**

```
向下滚动时：
┌────────────────────────────┐
│ Section 0: 静态头部         │
│ (向上滚出屏幕)             │
├────────────────────────────┤
│ [Overview][Details]...     │ ← 也向上滚动
│ (也会滚出屏幕)             │
├────────────────────────────┤
│ Page 内容                  │
│ (占满整个屏幕)             │
│ ...                        │
└────────────────────────────┘
```

**特点**:
- ✅ 更多内容显示空间
- ✅ 沉浸式体验
- ❌ 切换页面需要滚动回去
- 📱 **常见于**: YouTube 视频详情、Instagram 主页

---

## 🎨 视觉效果

### 吸顶模式 (pinToVisibleBounds = true)
```
初始状态:
┌──────────────────┐
│ 🎮 VC-Based      │ ← Section 0
├──────────────────┤
│[Ov][Det][Ana]    │ ← Segment Header
├──────────────────┤
│ Page 内容        │
│ • Item 1         │
│ • Item 2         │
└──────────────────┘

向下滚动后:
┌──────────────────┐
│[Ov][Det][Ana]    │ ← Header 吸附在顶部
├──────────────────┤
│ Page 内容        │
│ • Item 10        │
│ • Item 11        │
│ • Item 12        │
└──────────────────┘
```

### 跟随滚动 (pinToVisibleBounds = false)
```
初始状态:
┌──────────────────┐
│ 🎮 VC-Based      │ ← Section 0
├──────────────────┤
│[Ov][Det][Ana]    │ ← Segment Header
├──────────────────┤
│ Page 内容        │
│ • Item 1         │
│ • Item 2         │
└──────────────────┘

向下滚动后:
┌──────────────────┐
│ Page 内容        │ ← Header 已经滚出屏幕
│ • Item 10        │
│ • Item 11        │
│ • Item 12        │
│ • Item 13        │ ← 更多内容可见
└──────────────────┘
```

---

## 🎯 如何切换回吸顶模式

如果你想恢复吸顶效果，只需改回：

```swift
header.pinToVisibleBounds = true  // 恢复吸顶
```

---

## 💡 高级用法：根据滚动位置动态控制

你也可以根据滚动位置动态控制是否吸顶：

```swift
// 在 UIScrollViewDelegate 中
func scrollViewDidScroll(_ scrollView: UIScrollView) {
    let offsetY = scrollView.contentOffset.y
    
    // 当滚动超过 200pt 时才吸顶
    if offsetY > 200 {
        header.pinToVisibleBounds = true
    } else {
        header.pinToVisibleBounds = false
    }
}
```

但这需要重新创建 layout，比较复杂。

---

## 📝 修改的文件

- ✅ `ViewControllerBased/VCBasedPageViewController.swift`
- ✅ `CellBased/CellBasedPageViewController.swift`

两个方案都已修改，保持一致的行为。

---

## 🎉 验证

重新运行项目，现在：
1. 向下滚动时，Segment Header 会跟随内容一起向上移动
2. 继续滚动，Header 会完全消失
3. Page 内容会占据更多屏幕空间

如果想切换页面，需要先滚动回顶部才能看到 Segment。

---

现在 SegmentHeader 会跟随内容一起滚动了！🎉
